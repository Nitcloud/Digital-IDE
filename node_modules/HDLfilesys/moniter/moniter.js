"use strict";

const fs = require("fs");


fs.watch(filePath, { recursive : true }, function (event, fileName) {
    if (fileName) {
        if (fsWait) return;
        fsWait = setTimeout(() => {
          fsWait = false;
        }, 50);
        console.log(`${event}`);
        console.log(`${fileName}`);
        // this.onChange(fileName);
    }
});