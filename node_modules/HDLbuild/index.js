
let build = require("./kernel/build");
exports.build = build;

function synthFileLinter(verilogFilePath, parent) {
    let tty = build.synthLoadFile(verilogFilePath, parent);
    let message = tty.message;
    build.synthSystemRemoveFile()
}

function synth(path, style) {
    
}