const kernel = require('../../../src/HDLkernel')

suite('HDLKernel Test Suite', () => {
    test('test launch -> Simple Run', () => {
        // const module = new kernel();
        // module.launch();
    });
});